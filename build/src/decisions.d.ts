import * as types from './types';
export declare function createAttackDecision(actor: string, move: string): types.AttackDecision;
export declare function createSwitchDecision(actor: string, pokemon: string): types.SwitchDecision;
